#include "libft/libft.h"

int	main(void/*int argc, char **argv*/)
{
	//argc should be 3: program name, PID server and message
	// if (argc != 3)
		ft_printf("Check the program's parameters\n");
	//encript the message
	//send the message to server
	//create stop condition to know when it's finished
}
